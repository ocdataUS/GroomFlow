# Breadcrumb
- **Task asked:** Validate the staged admin UI refinements and line up next steps to close Sprint 1’s CRUD/settings work with UX polish.
- **Plan:** 1) Rebuild/install the plugin in Docker and pull authenticated HTML for Stages/Views to confirm helper text, removal warnings, and selector markup. 2) Capture QA outputs (including PHPCS) and note remaining compliance gaps. 3) Draft the implementation plan for finishing Sprint 1 (PHPCS fixes, interactive QA, docs updates).
- **Files changed:** _None (read-only QA + planning)._
- **Commands executed:** `bash scripts/build_plugin_zip.sh`; `docker compose up -d`; `docker compose cp ../build/bb-groomflow-0.1.0-dev.zip wordpress:/var/www/html/bb-groomflow-0.1.0-dev.zip`; `docker compose run --rm -T wpcli wp plugin install /var/www/html/bb-groomflow-0.1.0-dev.zip --force --activate`; `curl -c /tmp/wp-cookies.txt http://localhost:8083/wp-login.php`; `curl -b /tmp/wp-cookies.txt -c /tmp/wp-cookies.txt -d "log=codexadmin" -d "pwd=codexlocal" -d "wp-submit=Log In" -d "redirect_to=http://localhost:8083/wp-admin/" -d "testcookie=1" http://localhost:8083/wp-login.php`; `curl -s -b /tmp/wp-cookies.txt http://localhost:8083/wp-admin/admin.php?page=bbgf-stages > /tmp/bbgf-stages.html`; `curl -s -b /tmp/wp-cookies.txt http://localhost:8083/wp-admin/admin.php?page=bbgf-views > /tmp/bbgf-views.html`; `qa-phpcs plugin/bb-groomflow`.
- **Tests & results:** Manual curl pull confirms the Stages table renders helper tooltips (`bbgf-help`), usage callouts (`data-stage-usage`), and the drag-order controls; Views editor shows trimmed stage selector markup and badges. `qa-phpcs` report stored at `/opt/qa/artifacts/phpcs-1761687226.txt` flags outstanding `$wpdb->prepare` usage and formatting issues (notably in `class-stages-admin.php`, `class-plugin.php`, clients admin/list table).
- **Tips & Tricks:** Use a two-step curl (GET to set the test cookie, POST to log in) to grab authenticated admin markup for artifact review when headless screenshots stall; remember to reference `/tmp/wp-cookies.txt` for subsequent admin pulls. When PHPCS complains about interpolated table names inside `$wpdb->prepare`, wrap SQL builders in helper methods so we can safely whitelist the table reference or restructure the query without suppressing the sniff.
- **Remaining work:** Script or alternate headless/browser QA for Stages/Views to validate drag/drop and removal confirmations visually; resolve the PHPCS errors (prepare calls + formatting) before closing Sprint 1; log additional tips in `QA_TOOLBELT.md` after we settle on the headless QA approach; update `docs/sprints/Sprint-1.md` progress once fixes land.
