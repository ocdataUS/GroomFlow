# Breadcrumb
- **Task asked:** Take slice S1 to eliminate PHPCS warnings in plugin bootstrap, WP-CLI seeders, visit data service, and notifications service.
- **Plan:** Audit existing `qa-phpcs` output → remove blanket suppressions by fixing/preparing offending code → rerun tooling, rebuild ZIP, reinstall in Docker, and run manual admin happy-path before documenting the slice.
- **Files changed:** `plugin/bb-groomflow/includes/class-plugin.php`; `plugin/bb-groomflow/includes/cli/class-visits-command.php`; `plugin/bb-groomflow/includes/data/class-visit-service.php`; `plugin/bb-groomflow/includes/notifications/class-notifications-service.php`; `PROJECT_PLAN.md`; `docs/REFACTOR_LOG.md`; `docs/HISTORY.md`; `docs/NOTES.md`; `qa/QA_LOG.md`; `CHANGELOG.md`; `docs/breadcrumbs/2025-11-08-s1-phpcs.md` (this file).
- **Commands executed:** `qa-phpcbf <target files>`; `qa-phpcs plugin/bb-groomflow`; `bash scripts/build_plugin_zip.sh`; `docker compose cp ../build/bb-groomflow-0.1.0-dev.zip wordpress:/var/www/html/bb-groomflow.zip`; `docker compose run --rm -T wpcli wp plugin install /var/www/html/bb-groomflow.zip --force --activate`; `docker compose run --rm -T wpcli wp bbgf visits seed-demo --count=8 --force`.
- **Tests & results:** `qa-phpcs plugin/bb-groomflow` → `/opt/qa/artifacts/phpcs-1762654083.txt` (PASS); Manual admin happy-path (Clients, Guardians, Services, Packages, Flags, Views, Settings) → `/opt/qa/artifacts/manual-admin-happy-path-20251108.txt` (PASS after reinstall/seed).
- **Tips & Tricks:** Use `Schema::get_table_sql()` + `dbDelta()` for incremental upgrades so we avoid direct `ALTER TABLE` warnings; `wp_rand()` + `time()` keeps CLI demo data predictable and WPCS-friendly.
- **Remaining work:** None for S1; next slice can build on the clean PHPCS baseline.
