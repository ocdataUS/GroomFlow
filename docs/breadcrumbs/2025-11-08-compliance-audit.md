# Breadcrumb
- **Task asked:** Onboard via AGENTS.md, bring Docker WP online, and audit the GroomFlow plugin for WordPress compliance / refactor needs.
- **Plan:** Confirm documentation + sprint context → Build latest assets, package ZIP, install/seed inside Docker WP to mirror the QA workflow → Run QA toolbelt checks (PHPCS) and review key plugin modules for coding-standard, security, and architectural gaps → Summarize compliance status and next steps for refactors.
- **Files changed:** _None (read-only audit)._
- **Commands executed:** `docker compose up -d`; `npm run build`; `bash scripts/build_plugin_zip.sh`; `docker compose cp ../build/bb-groomflow-0.1.0-dev.zip wordpress:/var/www/html/bb-groomflow.zip`; `docker compose run --rm -T wpcli wp plugin install /var/www/html/bb-groomflow.zip --force --activate`; `docker compose run --rm -T wpcli wp bbgf visits seed-demo --count=8 --force`; `qa-phpcs plugin/bb-groomflow | tee /opt/qa/artifacts/phpcs-bbgf-20251108T174330.txt`.
- **Tests & results:** `qa-phpcs plugin/bb-groomflow` ⇒ `/opt/qa/artifacts/phpcs-bbgf-20251108T174330.txt` (multiple WordPress Coding Standards errors/warnings across `includes/class-plugin.php`, `includes/cli/class-visits-command.php`, `includes/data/class-visit-service.php`, `includes/notifications/class-notifications-service.php`). Manual UI QA not yet run pending compliance fixes.
- **Tips & Tricks:** Use `docker compose run --rm -T wpcli wp bbgf visits seed-demo --count=8 --force` to repopulate all views after reinstalling the ZIP.
- **Remaining work:** Address PHPCS violations (especially non-prepared SQL warnings, short ternaries, docblock gaps, rand/current_time usage); run full admin-form happy-path QA and smoke the Elementor/widget output once code cleanup lands; extend the audit to remaining admin/list-table classes for consistency.
