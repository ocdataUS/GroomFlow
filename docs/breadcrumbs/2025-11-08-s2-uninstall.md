# Breadcrumb
- **Task asked:** Land slice S2 by wiring a real uninstall routine that cleans GroomFlow tables/options and documenting the new safeguards.
- **Plan:** Implement schema-aware teardown (tables + options, multisite friendly) → expose preservation hooks (`BBGF_PRESERVE_DATA_ON_UNINSTALL`, `bbgf_allow_uninstall_cleanup`) → rebuild/install ZIP in Docker → run PHPCS and the full manual admin happy-path → update docs/logs/breadcrumb.
- **Files changed:** `plugin/bb-groomflow/uninstall.php`; `PROJECT_PLAN.md`; `docs/HISTORY.md`; `docs/NOTES.md`; `CHANGELOG.md`; `qa/QA_LOG.md`; `docs/REFACTOR_LOG.md`; `docs/breadcrumbs/2025-11-08-s2-uninstall.md`.
- **Commands executed:** `npm run build`; `bash scripts/build_plugin_zip.sh`; `docker compose cp ../build/bb-groomflow-0.1.0-dev.zip wordpress:/var/www/html/bb-groomflow.zip`; `docker compose run --rm -T wpcli wp plugin install /var/www/html/bb-groomflow.zip --force --activate`; `docker compose run --rm -T wpcli wp bbgf visits seed-demo --count=8 --force`; `qa-phpcs plugin/bb-groomflow`; `python3 /tmp/bbgf_manual_qa.py` (scripts the Clients→Settings happy-path); cleanup SQL for debug stage (`docker compose run --rm -T wpcli wp db query "DELETE FROM wp_7ptz4bz8ht_bb_stages WHERE stage_key LIKE 'debug-stage-%'"`).
- **Tests & results:** `qa-phpcs plugin/bb-groomflow` → `/opt/qa/artifacts/phpcs-1762654708.txt` (PASS); Manual admin happy-path (Clients, Guardians, Services, Packages, Flags, Views, Settings) → `/opt/qa/artifacts/manual-admin-happy-path-20251108-s2.txt` (PASS after ZIP reinstall/seed).
- **Tips & Tricks:** Define `BBGF_PRESERVE_DATA_ON_UNINSTALL` (or hook `bbgf_allow_uninstall_cleanup`) before uninstalling if you need to retain fixtures; the new routine iterates every site so large multisite installs should plan accordingly.
- **Remaining work:** None for S2—next slice can focus on bootstrap modularization.
