# Breadcrumb
- **Task asked:** Deliver slice S7 (Board Polling & REST Wiring) so the board renders real data on first paint and keeps shortcode/admin/Elementor shells in sync with REST payloads.
- **Plan:** Reuse localized settings from `Assets_Service` → render board markup from live payloads instead of placeholders → keep old placeholder path for Elementor samples → verify WP-CLI board payload + rebuild/install ZIP + qa-phpcs → update project docs/logs.
- **Files changed:** `plugin/bb-groomflow/includes/bootstrap/class-assets-service.php`; `plugin/bb-groomflow/includes/bootstrap/class-admin-menu-service.php`; `PROJECT_PLAN.md`; `docs/REFACTOR_LOG.md`; `docs/HISTORY.md`; `qa/QA_LOG.md`.
- **Commands executed:** `npm run build`; `bash scripts/build_plugin_zip.sh`; `cd docker && docker compose cp ../build/bb-groomflow-0.1.0-dev.zip wordpress:/var/www/html/bb-groomflow.zip`; `cd docker && docker compose run --rm -T wpcli wp plugin install /var/www/html/bb-groomflow.zip --force --activate`; `cd docker && docker compose run --rm -T wpcli wp bbgf visits seed-demo --count=8 --force`; `qa-phpcs plugin/bb-groomflow` → `/opt/qa/artifacts/phpcs-1762671186.txt`; `cd docker && docker compose run --rm -T wpcli wp eval '…build_board_payload…'` → `/opt/qa/artifacts/board-payload-20251109T005409.json`.
- **Tests & results:** PHPCS PASS; board payload JSON confirmed staged visits/capacity metadata; manual board spot-check (wp-admin + shortcode) now boots with live data post install.
- **Tips & Tricks:** Reuse the localized board settings when rendering PHP markup so filters like `bbgf_board_script_settings` stay in sync with the initial DOM; if the REST view has no visits, the renderer still surfaces empty columns via `build_board_payload`.
- **Remaining work:** None for S7 — next slice can focus on modal editing flows or lobby token handling.
