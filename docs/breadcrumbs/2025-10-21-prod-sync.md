# Breadcrumb
- **Task asked:** Mirror production into Docker without symlinks, so future agents inherit a turnkey dev environment.
- **Plan:** 1) Sync `wp-content` + DB from production. 2) Start isolated Docker stack. 3) Copy assets into volumes and import DB. 4) Localize URLs, create dev admin. 5) Document the workflow and automation script.
- **Files changed:** .gitignore; CHANGELOG.md; AGENTS.md; docker/docker-compose.yml; docker/README.md; docs/workflow.md; scripts/load_prod_snapshot.sh; docs/breadcrumbs/2025-10-21-prod-sync.md.
- **Commands executed:** sshpass tar stream for wp-content; `~/.secrets/codex_bubbles_ssh.sh wp db export -`; `docker compose up -d`; `docker compose cp` for wp-content; `cat database.sql | docker compose exec -T db mysql`; `docker compose run --rm wpcli wp search-replace ...`; `bash scripts/load_prod_snapshot.sh` (validation run).
- **Tests & results:** `curl -I http://localhost:8083` returned 200; `docker compose run --rm wpcli wp option get siteurl` now outputs local URL.
- **Tips & Tricks:** Use `scripts/load_prod_snapshot.sh` after refreshing `docker/prod-sync/` to rebuild the local snapshot in minutes (no bind mounts, all copies).
- **Remaining work:** Begin Sprint 0 implementation atop the synchronized environment; monitor object-cache behavior locally.
