# Breadcrumb
- **Task asked:** Replace the repo’s slice-heavy PM ritual with an Asana-first task model that fits Codex / GPT-5.4 better, then simplify git back to a single-branch workflow because this project is only being worked by one person.
- **Plan:** Audit the current repo ritual and live Asana structure, review official Codex/GPT-5.4 best-practice guidance, redesign the active docs around one-task/one-thread continuity, wire the new model into the context pack, mirror the repo-wide rules into Asana, then merge all local work into `main` and delete the extra branch.
- **Asana:** Reworked the GroomFlow project lanes to `Inbox`, `Ready`, `Active`, `Blocked`, `PM Review`, and `Closed`; created and locked `Redesign repo PM model around Asana + Codex` (`1213732779650114`); rewrote that task into the new `Goal` / `Context` / `Constraints` / `Done When` format; created project brief `1213718552434906` (`GroomFlow Operating Model`); reopened the task to land the single-branch decision; kept `Stabilization: repo tidy, full QA/QC, onboarding and handoff ritual hardening` (`1213718549374304`) in `PM Review`.
- **Files changed:** `AGENTS.md`, `README.md`, `SPEC.md`, `docs/README.md`, `docs/OPERATING_MODEL.md`, `docs/ASANA_TOOLBOX.md`, `docs/CHANGE_MANAGEMENT.md`, `docs/HISTORY.md`, `docs/REFACTOR_LOG.md`, `docs/workflow.md`, `scripts/generate_context_pack.sh`, `AGENT_HANDOFF.md`, and this breadcrumb.
- **External guidance used:** Reviewed official OpenAI Codex / GPT-5.4 guidance from `developers.openai.com` and translated it into repo workflow rules: short `AGENTS.md`, one coherent thread per task, explicit `Goal` / `Context` / `Constraints` / `Done When`, durable process in docs, live state in Asana, and automation only after the manual workflow is stable.
- **Commands executed:** `git status --short --branch`; `git diff`; `rg`; `sed`; `ocasana sections list`; `ocasana tasks list/show/comments-list/update/move/comment`; `ocasana projects briefs-get`; `ocasana help projects briefs-create`; `ocasana help projects briefs-update`; `ocasana help tasks update`; `git diff --check`; `bash scripts/generate_context_pack.sh`; `git checkout main`; `git merge --ff-only refactor/stabilization-qa-handoff-rituals`; `git branch -d refactor/stabilization-qa-handoff-rituals`.
- **Validation:** `git diff --check` (PASS); `bash scripts/generate_context_pack.sh` (PASS, `docs/context/context-pack.json` now includes `docs.operating_model`, `asana.project_brief`, `asana.inbox`, `asana.ready`, `asana.active`, `asana.blocked`, `asana.pm_review`, and `asana.closed_recent`); `git branch -a` now shows only local `main` plus `origin/main`.
- **Continuity notes:** The new model treats Asana as the live PM system, `AGENT_HANDOFF.md` as one current resume snapshot, breadcrumbs as optional durable journals, `docs/REFACTOR_LOG.md` and `docs/HISTORY.md` as legacy archives, and `docs/context/context-pack.json` as the compaction packet carrying the operating model plus live Asana state and the project brief. Git now defaults to `main`; separate branches are no longer part of the standard ritual.
- **Exact next step:** Start the next task from `main` after auditing the Asana project brief plus `Active`, `Blocked`, `PM Review`, and `Ready`.
