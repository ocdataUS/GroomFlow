# Breadcrumb
- **Task asked:** Re-onboard the repo, recover the interrupted slice, harden the onboarding/handoff ritual around Asana + compaction, and run a full Docker QA/QC pass without losing context again.
- **Plan:** Audit Asana and the stale handoff first, document a mandatory Asana/task audit + compaction ritual, fix the broken QA helpers, verify the interrupted Manage Visits slice in Docker, and leave the branch committed + ready for review.
- **Asana:** Created and locked `Stabilization: repo tidy, full QA/QC, onboarding and handoff ritual hardening` (`1213718549374304`) in GroomFlow/Doing because the local Manage Visits work had no active Asana task.
- **Files changed:** `AGENTS.md`, `README.md`, `SPEC.md`, `docs/README.md`, `docs/ASANA_TOOLBOX.md`, `docs/workflow.md`, `scripts/generate_context_pack.sh`, `scripts/qa_fast.sh`, `scripts/qa_smoke.sh`, `AGENT_HANDOFF.md`, `qa/QA_LOG.md`, this breadcrumb, plus the interrupted Manage Visits slice under `plugin/bb-groomflow/includes/admin/`, `plugin/bb-groomflow/includes/data/`, `plugin/bb-groomflow/includes/bootstrap/`, `plugin/bb-groomflow/includes/class-plugin.php`, `plugin/bb-groomflow/assets/js/manage-visits.js`, and `plugin/bb-groomflow/assets/css/admin.css`.
- **Commands executed:** `git status --short --branch`; `git fetch origin`; `ocasana tasks list/show/comments/attachments/create/comment/update/move`; `bash scripts/qa_fast.sh`; `bash scripts/qa_smoke.sh`; `node scripts/capture_artifacts.js after`; browser-admin QA for Manage Visits details/export/reopen; `qa-pa11y http://localhost:8083`; `qa-pa11y http://localhost:8083/groomflow-staff-board/`; `qa-screenshot http://localhost:8083`; `qa-screenshot http://localhost:8083/contact-us/`; `docker compose -f docker/docker-compose.yml run --rm -T wpcli wp eval 'bbgf()->visit_service()->checkout_visit( 96, 3, "QA checkout fixture" );'` as a disposable QA fixture before reopening the visit through the admin UI.
- **Tests & results:**
  - `bash scripts/qa_fast.sh` → `/opt/qa/artifacts/phpcs-1773802533.txt` (PASS after fixing `scripts/qa_fast.sh` absolute-path handling).
  - `bash scripts/qa_smoke.sh` → `/opt/qa/artifacts/qa-smoke-20260318T025817Z.txt`, `/opt/qa/artifacts/phpcs-1773802713.txt` (PASS after stubbing outbound mail in `scripts/qa_smoke.sh` so notification smoke no longer hangs on the persisted prototype).
  - Manual admin happy-path → `/opt/qa/artifacts/stabilization-qa-20260318T0258/manual-admin-happy-path-20260318T0258.txt` (PASS for Stages, Guardians, Clients, Services, Packages, Flags, Views, Settings).
  - Public board/modal captures → `/opt/qa/artifacts/stabilization-qa-20260318T0258/board-after-*.png`, `/opt/qa/artifacts/stabilization-qa-20260318T0258/modal-after-*.png` (PASS).
  - Admin browser QC → `/opt/qa/artifacts/stabilization-qa-20260318T0258/admin-summary.json` plus admin screenshots/CSV exports (PASS; Manage Visits filter, details row, both CSV exports, and Uncheckout → reopen flow all verified. Reopen redirects back to the filtered URL with `bbgf_message=visit-reopened`, and the row reappears as active on the base list.)
  - `qa-pa11y` → `/opt/qa/artifacts/pa11y-1773803582.json`, `/opt/qa/artifacts/pa11y-1773803572.json` (WARN; 22 public-theme accessibility issues, mostly linked-logo alt text and contrast failures. The wrapper did not produce a distinct authenticated board audit.)
  - Public site screenshots → `/opt/qa/artifacts/stabilization-qa-20260318T0258/site-home.png`, `/opt/qa/artifacts/stabilization-qa-20260318T0258/site-contact-us.png` (WARN; contact page renders, but the home page shows blank/placeholder sections below the hero in the current prototype.)
- **Continuity notes:** Docker is running the persisted WordPress volume at `6.8.3` even though `docker/docker-compose.yml` pins `wordpress:6.9.0-php8.2-apache`; the Manage Visits prototype in Docker matches the interrupted local slice; `bash scripts/generate_context_pack.sh` now captures Asana ToDo/Doing/Done plus current branch/git status for future re-onboarding.
