# Breadcrumb
- **Task asked:** Restore admin emoji picker without relying on `__experimentalEmojiPicker`, verify settings colors persist, and QA each GroomFlow admin screen in Docker.
- **Plan:** 1) Load vendor emoji picker bundle and swap React integration for custom element fallback. 2) Re-test settings persistence and board localization. 3) Hit Flags/Services/Packages/Views/Settings screens in Docker to confirm notices and tables render once.
- **Files changed:** package.json; package-lock.json; plugin/bb-groomflow/assets/css/admin.css; plugin/bb-groomflow/assets/js/admin.js; plugin/bb-groomflow/assets/js/vendor/emoji-picker-element.js; plugin/bb-groomflow/includes/admin/class-flags-admin.php; plugin/bb-groomflow/includes/admin/class-packages-admin.php; plugin/bb-groomflow/includes/admin/class-services-admin.php; plugin/bb-groomflow/includes/admin/class-settings-admin.php; plugin/bb-groomflow/includes/admin/class-views-admin.php; build/bb-groomflow-0.1.0-dev.zip (regenerated).
- **Commands executed:** npm install emoji-picker-element@1.21.1 --save-dev; npx esbuild …/emoji-picker-element.js; npm install puppeteer@22.13.0 --save-dev; bash scripts/build_plugin_zip.sh; docker compose up -d; docker compose run --rm -T wpcli wp plugin install /var/www/html/bb-groomflow-0.1.0-dev.zip --force --activate; multiple curl requests for admin pages; docker compose run --rm -T wpcli wp option update/get/delete bbgf_settings; docker compose run --rm -T wpcli wp eval …; git status (read-only).
- **Tests & results:** Verified vendor script loads on admin pages via curl; confirmed `bbgfBoardSettings` reflects stored colors using wp-cli eval; rebuilt ZIP and reinstalled in Docker. Puppeteer smoke screenshot attempt timed out (headless Chrome in container); no visual artifact captured.
- **Tips & Tricks:** Use `wp option update … --format=json` to populate complex arrays during QA instead of editing forms manually; when bundling third-party modules for admin, add a dedicated vendor handle and make main script depend on it.
- **Remaining work:** Headless browser QA still failing due to Chromium handshake timeout—lean on Chrome CDP/QA toolbelt helpers if visual proof is required. Await feedback on removing legacy docs (CLAUDE.md) noted by product.***
