# Breadcrumb
- **Task asked:** S11: Elementor/Shortcode controls completion (Asana `1212222478675446`)
- **Plan:** Review current shortcode/Elementor wiring against Sprint 4 deliverables (metadata ordering, style knobs, lobby/display overrides, preview parity), design scope, then implement controls + CSS var theming, rebuild ZIP, install in Docker, run qa-phpcs + admin/board QA, and update docs/Asana.
- **Files changed:** `plugin/bb-groomflow/includes/bootstrap/class-assets-service.php`, `plugin/bb-groomflow/includes/elementor/class-board-widget.php`, `plugin/bb-groomflow/includes/data/class-visit-service.php` (carryover), `plugin/bb-groomflow/assets/src/index.js`, `plugin/bb-groomflow/assets/src/style.scss`, `plugin/bb-groomflow/assets/build/board.{js,js.map,css}`, `docs/NOTES.md`, `docs/REFACTOR_LOG.md`, `qa/QA_LOG.md`, `docs/breadcrumbs/2025-12-02-s11-elementor-polish.md`, `docs/breadcrumbs/2025-12-02-visit-service-hardening.md` (carryover)
- **Commands executed:** `eval "$(ssh-agent -s)" && ssh-add ~/.ssh/id_ed25519_codex`; `ssh -T git@github.com`; `git remote set-url origin git@github.com:ocdataUS/GroomFlow.git`; `git status --short`; `ocasana --profile ocdata workspaces/projects/sections/tasks list/show/comments-list/comment/update`; `git checkout -b refactor/s11-elementor-polish`; `npm run build`; `qa-phpcbf plugin/bb-groomflow/includes/bootstrap/class-assets-service.php` (avoid passing multiple paths; see tips); `bash scripts/build_plugin_zip.sh`; `docker compose up -d`; `docker compose cp ../build/bb-groomflow-0.1.0-dev.zip wordpress:/var/www/html/bb-groomflow.zip`; `docker compose run --rm -T wpcli wp plugin install /var/www/html/bb-groomflow.zip --force --activate`; `docker compose run --rm -T wpcli wp bbgf visits seed-demo --count=8 --force`; `qa-phpcs plugin/bb-groomflow`; manual admin happy-path via WP-CLI edits; `docker compose run --rm -T wpcli wp bbgf visits list --limit=5`
- **Tests & results:** `qa-phpcs plugin/bb-groomflow` → `/opt/qa/artifacts/phpcs-1764699521.txt` (PASS); Manual admin happy-path (WP-CLI updates Clients→Settings) → `/opt/qa/artifacts/manual-admin-happy-path-20251202T181902.txt` (PASS); Board list smoke (`wp bbgf visits list --limit=5`) → `/opt/qa/artifacts/board-list-20251202T182008.txt` (PASS)
- **Tips & Tricks:** Operations Clarity workspace requires `--profile ocdata` with `ocasana`; custom field `GF Status` options resolved via `projects custom-fields-list --json`. Missing docs noted: `docs/DEBT.md`, `docs/ASANA_TOOLBOX.md`. `qa-phpcbf` treats the second argument as the artifact path—passing multiple paths will clobber the next file (had to restore the Elementor widget after learning this).
- **Remaining work:** Monitor Asana/PM review; keep an eye on carryover Visit_Service changes from prior slice.
