# Breadcrumb
- **Task asked:** Land slice S3 by modularizing the plugin bootstrap (assets/Elementor, admin menu shell, REST controllers, WP-CLI hooks) so `class-plugin.php` stops carrying PHPCS ignores and is easier to test.
- **Plan:** Add dedicated services under `includes/bootstrap/` → wire them into `Plugin` (wrappers for public helpers) → rebuild/install ZIP in Docker → run `qa-phpcs` + manual admin happy-path → update docs/logs/breadcrumb.
- **Files changed:** `plugin/bb-groomflow/includes/class-plugin.php`; `plugin/bb-groomflow/includes/bootstrap/class-assets-service.php`; `class-admin-menu-service.php`; `class-rest-service.php`; `class-cli-service.php`; `plugin/bb-groomflow/includes/bootstrap/index.php`; `PROJECT_PLAN.md`; `docs/HISTORY.md`; `docs/NOTES.md`; `CHANGELOG.md`; `docs/REFACTOR_LOG.md`; `qa/QA_LOG.md`; `docs/breadcrumbs/2025-11-08-s3-plugin-bootstrap-modularization.md`.
- **Commands executed:** `bash scripts/build_plugin_zip.sh`; `docker compose cp ../build/bb-groomflow-0.1.0-dev.zip wordpress:/var/www/html/bb-groomflow.zip`; `docker compose run --rm -T wpcli wp plugin install /var/www/html/bb-groomflow.zip --force --activate`; `docker compose run --rm -T wpcli wp bbgf visits seed-demo --count=8 --force`; `qa-phpcs plugin/bb-groomflow`.
- **Tests & results:** `qa-phpcs plugin/bb-groomflow` → `/opt/qa/artifacts/phpcs-1762665491.txt` (PASS); Manual admin happy-path (Clients, Guardians, Services, Packages, Flags, Views, Settings + dashboard shell) → `/opt/qa/artifacts/manual-admin-happy-path-20251108-s3.txt` (PASS).
- **Tips & Tricks:** New services live under `includes/bootstrap/`; call `bbgf()->bootstrap_elementor()` or `bbgf()->get_placeholder_board_markup()` if you need the legacy helpers—they proxy to `Assets_Service`. Extend those services instead of inflating `Plugin` for future slices.
- **Remaining work:** None for S3; next slice can tackle CLI/seed hardening (S4) on top of the modularized bootstrap.
