# DEBT — GroomFlow

- Board caching TTLs are filter-only (`bbgf_board_payload_cache_ttl`, `bbgf_board_metadata_cache_ttl`, `bbgf_board_views_cache_ttl`); add a UI toggle or setting if product wants admins to tune/disable caching without code once load patterns are clearer.
