#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
TARGET="${1:-.}"
OUT="${2:-/opt/qa/artifacts/phpcs-$(date +%s).txt}"

phpcs --standard="${ROOT_DIR}/phpcs.xml.dist" --extensions=php,inc,module,theme,plugin "${TARGET}" | tee "${OUT}"
echo "${OUT}"
